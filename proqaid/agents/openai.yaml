interface:
  display_name: "PROQAID"
  short_description: "Parallel governed software delivery"
  default_prompt: "Use $proqaid to coordinate this software iteration with useful single-layer Role Agent and Worker parallelism, test the delivered candidate in a ready test environment, and review only the final documents."
