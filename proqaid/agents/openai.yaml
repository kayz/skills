interface:
  display_name: "PROQAID"
  short_description: "User-selected governed project delivery"
  default_prompt: "Use $proqaid with the user's explicit Lite selection, or Full by default, then run the iteration through implementation, independent quality, audit, and cleanup."
