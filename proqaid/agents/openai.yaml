interface:
  display_name: "PROQAID"
  short_description: "Governed, tool-first project delivery"
  default_prompt: "Use $proqaid with the user's explicit Lite selection, or Full by default. Bound iteration size, invoke only affected roles, batch Human Operator prerequisites, use existing deterministic tools, then deliver candidate-bound acceptance and cleanup."
